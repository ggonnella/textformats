datatypes:
  identifier: {regex: "[a-zA-Z_][a-zA-Z0-9_]*"}
  hex_digit: {regex: "[0-9A-Fa-f]"}
  hex_byte: {regex: "[0-9A-Fa-f]{2}"}
  hex_color:
    composed_of:
      - red: hex_byte
      - green: hex_byte
      - blue: hex_byte
      - alpha: hex_byte
    last_optional: true

