datatypes:
  u_too_short:
    one_of:
      - {constant: "a"}
  u_empty:
    one_of: []
  u_map:
    one_of: {constant: "a"}
  u_scalar:
    one_of: a
  u_unknown_key:
    one_of:
      - {constant: "a"}
      - json
    x: 0
  u_w_too_less_labels:
    one_of:
      - {constant: {"a": true}}
      - json
      - string
    wrapped: true
    type_labels: ["json", "string"]
  u_w_too_many_labels:
    one_of:
      - {constant: {"a": true}}
      - json
      - string
    wrapped: true
    type_labels: ["a", "json", "string", "b"]
  u_w_labels_not_string:
    one_of:
      - {constant: {"a": true}}
      - json
      - string
    wrapped: true
    type_labels: ["a", "json", ["string"]]
