# Package

version       = "1.0.0"
author        = "Giorgio Gonnella"
description   = "Python bindings for the TextFormats library"
license       = "ISC"

# Dependencies

requires "nim >= 1.2.6",
         "cligen >= 1.0",
         "yaml >= 0.14",
         "regex >= 0.15",
         "nimpy",
         "textformats"

